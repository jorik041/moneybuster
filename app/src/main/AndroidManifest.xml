<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.eneiluj.ihatemoney">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- For auto start -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- For web sync retries -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Needed if app targets API >= 21 -->
    <uses-feature android:name="android.hardware.location.gps" />
    <uses-feature android:name="android.hardware.location.network" />

    <application
        android:name="net.eneiluj.ihatemoney.util.IHateMoney"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="net.eneiluj.ihatemoney.android.activity.SplashscreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name="net.eneiluj.ihatemoney.android.activity.BillsListViewActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
            <meta-data
                android:name="android.app.default_searchable"
                android:value="net.eneiluj.ihatemoney.android.activity.LogjobsListViewActivity" />
        </activity>

        <activity
            android:name="net.eneiluj.ihatemoney.android.activity.SettingsActivity"
            android:label="@string/settings_server_settings"
            android:parentActivityName="net.eneiluj.ihatemoney.android.activity.PreferencesActivity"
            android:windowSoftInputMode="stateVisible" />

        <activity
            android:name="net.eneiluj.ihatemoney.android.activity.PreferencesActivity"
            android:label="@string/action_settings"
            android:parentActivityName="net.eneiluj.ihatemoney.android.activity.BillsListViewActivity"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/Preferences"
            />

        <activity
            android:name="net.eneiluj.ihatemoney.android.activity.NewProjectActivity"
            android:label="@string/simple_add_project"
            android:parentActivityName="net.eneiluj.ihatemoney.android.activity.BillsListViewActivity"
            android:windowSoftInputMode="stateHidden"
            android:launchMode="singleTask">
        </activity>

        <activity
            android:name="net.eneiluj.ihatemoney.android.activity.EditPhoneTrackLogjobActivity"
            android:label="@string/simple_edit_pt"
            android:parentActivityName="net.eneiluj.ihatemoney.android.activity.BillsListViewActivity"
            android:windowSoftInputMode="stateHidden"
            android:launchMode="singleTask">
            <!-- this is to receive share action from other apps -->
            <intent-filter android:label="@string/action_create_share">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name="net.eneiluj.ihatemoney.android.activity.EditCustomLogjobActivity"
            android:label="@string/simple_edit_cu"
            android:parentActivityName="net.eneiluj.ihatemoney.android.activity.BillsListViewActivity"
            android:windowSoftInputMode="stateHidden"
            android:launchMode="singleTask">
            <!-- this is to receive share action from other apps -->
            <intent-filter android:label="@string/action_create_custom_share">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name="net.eneiluj.ihatemoney.android.activity.AboutActivity"
            android:label="@string/simple_about"
            android:parentActivityName="net.eneiluj.ihatemoney.android.activity.BillsListViewActivity" />

        <service
            android:name="net.eneiluj.ihatemoney.android.quicksettings.NewLogjobTileService"
            android:icon="@drawable/ic_quicksettings_new"
            android:label="@string/action_create"
            android:description="@string/action_create"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name="net.eneiluj.ihatemoney.service.LoggerService"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name="net.eneiluj.ihatemoney.service.WebTrackService"
            android:exported="false" />

        <receiver
            android:name="net.eneiluj.ihatemoney.service.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
