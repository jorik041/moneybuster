image: livexp/android-sdk

variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
  - build

before_script:
    - docker-android-sdk-install "build-tools;28.0.3"
    - cd /root/
    - wget https://services.gradle.org/distributions/gradle-5.2.1-bin.zip > /dev/null 2>&1
    - unzip gradle-5.2.1-bin.zip > /dev/null 2>&1
    - cd -

build_app:
  stage: build
  only:
    refs:
      - build
  artifacts:
    paths:
      - app/build/outputs/apk/dev/debug/app-dev-debug.apk
  script:
      - export PATH=/root/gradle-5.2.1/bin:$PATH
      - pwd
      - cd app ; gradle assembleDev

